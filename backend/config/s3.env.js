const env = {
    aws_access_key_id: '*',
    aws_secret_access_key  : '*',
    REGION: '*',
    Bucket: '*',
    aws_config_file: AWS_SDK_LOAD_CONFIG=1
    // AWS_CONFIG_FILE : AWS_SDK_LOAD_CONFIG=1,
}

module.exports = env;

